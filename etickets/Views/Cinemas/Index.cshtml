@model IEnumerable<Cinema>
@{
    ViewData["Title"] = "List of Ci";
}
<div class="row">
    <div class="col-md-8 offset - md-2">

        <table class="table">
            <thead>

                <tr class=" text-center">
                    <th> Html.DisplayNameFor (model= > model.Logo)</th>
                    <th> Html.DisplayNameFor (model= > model.Name)</th>
                    <th> Html.DisplayNameFor (model= > model.Despriction)</th>
                    <th> Actions </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var iteam in Model)
                {
                    <tr>
                        <td>
                            class = "align-middle">
                            <img class="rouded-cicle" src="@iteam.logo" alt="@iteam.name" style="max-width: 150px" />
                        </td>
                        <td class="" "align-middle">
                            Html.DisplayFor (modeliteam = > iteam.Name)
                        </td>
                        <td class="" "align-middle">
                            Html.DisplayFor (modeliteam = > iteam.Despriction)
                        </td>
                        <td class="" "align-middle">
                            <a class="btn btn-outline-primary"> <i class="bi bi-pencil-square"> </i> Edit </a> |
                            <a class="btn btn-outline-info"> <i class="bi bi-eye"> </i> Details </a>  |
                            <a class="btn btn-danger text-white"> <i class="bi bi-trash"> </i> Delete </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>








<h1> Welcome to the Actors View - Index </h1>
